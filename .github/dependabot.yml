# Actions are pinned to commit SHAs (zizmor unpinned-uses); Dependabot keeps
# the pins current. Cargo dependencies are deliberately version-pinned and
# excluded — see the note at the top of the Makefile.
version: 2
updates:
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
